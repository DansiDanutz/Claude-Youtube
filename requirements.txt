# Python deps for the video-editor pipeline tools (Python 3.10+).
# Install into a venv at the repo root:
#     python -m venv venv
#     venv/Scripts/python -m pip install -r requirements.txt      # Windows
#     ./venv/bin/pip install -r requirements.txt                  # macOS/Linux
# Then run tools as `python tools/<tool>.py ...` with that venv active.
#
# Also required on PATH (not pip): ffmpeg + ffprobe, and Node.js (for the Remotion render step).
# tools/crawl_web.py also wants the Crawl4AI CLI on PATH (its own global install, not a venv dep):
#     pipx install "crawl4ai[all]" && crawl4ai-setup

requests==2.34.2                  # transcribe.py
Pillow==12.3.0                    # gen_thumbnail.py (image handling)
google-genai==2.14.0              # gen_thumbnail.py (Gemini / Nano Banana Pro)

# YouTube stats + upload (tools/yt_stats.py, tools/yt_upload.py) — OAuth via .youtube/ token files:
google-api-python-client==2.198.0
google-auth==2.56.2
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0

# Optional — only for tools/capture_web.py (auto-capturing public web pages to clone as beats):
#   pip install playwright && playwright install chromium
# playwright
